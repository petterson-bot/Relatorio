<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SDA 2026 · Dashboard Executivo</title>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;600;700;800&display=swap" rel="stylesheet">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/4.4.1/chart.umd.min.js"></script>
    <style>
        :root {
            --bg-body: #050A05;
            --bg-card: #0B130B;
            --accent: #4AE54A; /* Verde Neon das referências */
            --accent-dim: rgba(74, 229, 74, 0.15);
            --text-main: #FFFFFF;
            --text-dim: #88A088;
            --border: #1A351A;
            --danger: #FF4444;
            --warning: #FFD700;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            background-color: var(--bg-body);
            color: var(--text-main);
            font-family: 'Montserrat', sans-serif;
            line-height: 1.6;
            -webkit-font-smoothing: antialiased;
        }

        /* Estilo Clean para o Header */
        header {
            padding: 40px 20px;
            text-align: center;
            border-bottom: 1px solid var(--border);
            background: linear-gradient(180deg, #0D1A0D 0%, var(--bg-body) 100%);
            position: relative;
        }

        header h1 {
            font-size: 28px;
            font-weight: 800;
            letter-spacing: -0.5px;
            color: var(--accent);
            text-transform: uppercase;
        }

        header p {
            color: var(--text-dim);
            font-size: 13px;
            margin-top: 5px;
            font-weight: 400;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 30px 20px;
        }

        /* Botão de Download Estilizado */
        .btn-dl {
            position: absolute;
            top: 30px;
            right: 30px;
            background: transparent;
            border: 1px solid var(--accent);
            color: var(--accent);
            padding: 8px 16px;
            font-size: 11px;
            font-weight: 700;
            text-transform: uppercase;
            cursor: pointer;
            border-radius: 4px;
            transition: all 0.3s ease;
        }

        .btn-dl:hover {
            background: var(--accent);
            color: var(--bg-body);
        }

        /* Navegação em Tabs Clean */
        .nav-tabs {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-bottom: 40px;
        }

        .tab-btn {
            background: none;
            border: none;
            color: var(--text-dim);
            font-family: 'Montserrat', sans-serif;
            font-weight: 600;
            font-size: 14px;
            cursor: pointer;
            padding: 10px 5px;
            position: relative;
            transition: color 0.3s;
        }

        .tab-btn.active {
            color: var(--accent);
        }

        .tab-btn.active::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 100%;
            height: 2px;
            background: var(--accent);
        }

        /* Grid de KPIs */
        .kpi-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
            gap: 20px;
            margin-bottom: 40px;
        }

        .kpi-card {
            background: var(--bg-card);
            padding: 25px;
            border-radius: 8px;
            border: 1px solid var(--border);
            text-align: left;
        }

        .kpi-card span {
            display: block;
            font-size: 10px;
            font-weight: 700;
            color: var(--text-dim);
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 8px;
        }

        .kpi-card h2 {
            font-size: 24px;
            font-weight: 700;
            color: var(--text-main);
        }

        /* Tabelas Minimalistas */
        .table-container {
            background: var(--bg-card);
            border-radius: 8px;
            border: 1px solid var(--border);
            overflow: hidden;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            font-size: 13px;
        }

        th {
            background: rgba(255,255,255,0.02);
            text-align: left;
            padding: 15px;
            color: var(--accent);
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            cursor: pointer;
            border-bottom: 1px solid var(--border);
        }

        td {
            padding: 15px;
            border-bottom: 1px solid var(--border);
            color: var(--text-main);
        }

        tr:last-child td { border-bottom: none; }
        tr:hover { background: var(--accent-dim); }

        .content-section { display: none; animation: fadeIn 0.4s ease forwards; }
        .content-section.active { display: block; }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(5px); }
            to { opacity: 1; transform: translateY(0); }
        }
    </style>
</head>
<body>

<header>
    <button class="btn-dl" onclick="downloadHTML()">Salvar Cópia</button>
    <h1>SDA 2026 · <span style="font-weight:300">Dashboard</span></h1>
    <p>RELATÓRIO DE PERFORMANCE · CONTA 04 · Samuel MKT</p>
</header>

<div class="container">
    <div class="nav-tabs">
        <button class="tab-btn active" onclick="showTab('resumo')">RESUMO</button>
        <button class="tab-btn" onclick="showTab('campanhas')">CAMPANHAS</button>
        <button class="tab-btn" onclick="showTab('comercial')">COMERCIAL</button>
    </div>

    <div id="resumo" class="content-section active">
        <div class="kpi-grid">
            <div class="kpi-card"><span>Investimento</span><h2>R$ 250.932</h2></div>
            <div class="kpi-card"><span>Faturamento</span><h2 style="color:var(--accent)">R$ 478.406</h2></div>
            <div class="kpi-card"><span>ROAS Geral</span><h2>1.91x</h2></div>
            <div class="kpi-card"><span>Ticket Médio</span><h2>R$ 1.128</h2></div>
        </div>
    </div>

    <div id="campanhas" class="content-section">
        <div class="table-container">
            <table id="tblCamp">
                <thead>
                    <tr>
                        <th onclick="sortTable(0, 'tblCamp')">Campanha</th>
                        <th onclick="sortTable(1, 'tblCamp')">Investido</th>
                        <th onclick="sortTable(2, 'tblCamp')">Leads</th>
                        <th onclick="sortTable(3, 'tblCamp')">Vendas</th>
                        <th onclick="sortTable(4, 'tblCamp')">ROAS</th>
                    </tr>
                </thead>
                <tbody>
                    <tr><td>Checkout Quente ABO</td><td>R$ 23.206</td><td>16</td><td>20</td><td>0.98x</td></tr>
                    <tr><td>Advantage CBO T04</td><td>R$ 9.926</td><td>24</td><td>8</td><td>0.85x</td></tr>
                    <tr><td>Lista Esp. Quente</td><td>R$ 939</td><td>31</td><td>22</td><td>21.8x</td></tr>
                    <tr><td>Frio ABO T03</td><td>R$ 14.977</td><td>786</td><td>3</td><td>0.33x</td></tr>
                </tbody>
            </table>
        </div>
    </div>

    <div id="comercial" class="content-section">
        <div class="table-container">
            <table id="tblClosers">
                <thead>
                    <tr>
                        <th onclick="sortTable(0, 'tblClosers')">Closer</th>
                        <th onclick="sortTable(1, 'tblClosers')">Vendas</th>
                        <th onclick="sortTable(2, 'tblClosers')">Receita</th>
                    </tr>
                </thead>
                <tbody>
                    <tr><td>Nathalia</td><td>36</td><td>R$ 50.940</td></tr>
                    <tr><td>Juliana</td><td>35</td><td>R$ 43.535</td></tr>
                    <tr><td>Jalai</td><td>32</td><td>R$ 43.001</td></tr>
                    <tr><td>Junior</td><td>34</td><td>R$ 41.045</td></tr>
                </tbody>
            </table>
        </div>
    </div>
</div>

<script>
    // Navegação de Abas
    function showTab(tabId) {
        document.querySelectorAll('.content-section').forEach(section => section.classList.remove('active'));
        document.querySelectorAll('.tab-btn').forEach(btn => btn.classList.remove('active'));
        document.getElementById(tabId).classList.add('active');
        event.currentTarget.classList.add('active');
    }

    // Ordenação de Tabelas Dinâmica
    function sortTable(n, tableId) {
        let table = document.getElementById(tableId);
        let rows, switching, i, x, y, shouldSwitch, dir, switchcount = 0;
        switching = true;
        dir = "asc";
        while (switching) {
            switching = false;
            rows = table.rows;
            for (i = 1; i < (rows.length - 1); i++) {
                shouldSwitch = false;
                x = rows[i].getElementsByTagName("TD")[n];
                y = rows[i + 1].getElementsByTagName("TD")[n];
                
                let xVal = x.innerHTML.replace(/[R$\.x]/g, '').replace(',', '.').trim();
                let yVal = y.innerHTML.replace(/[R$\.x]/g, '').replace(',', '.').trim();
                
                let xFinal = isNaN(parseFloat(xVal)) ? x.innerHTML.toLowerCase() : parseFloat(xVal);
                let yFinal = isNaN(parseFloat(yVal)) ? y.innerHTML.toLowerCase() : parseFloat(yVal);

                if (dir == "asc") {
                    if (xFinal > yFinal) { shouldSwitch = true; break; }
                } else if (dir == "desc") {
                    if (xFinal < yFinal) { shouldSwitch = true; break; }
                }
            }
            if (shouldSwitch) {
                rows[i].parentNode.insertBefore(rows[i + 1], rows[i]);
                switching = true;
                switchcount++;
            } else if (switchcount == 0 && dir == "asc") {
                dir = "desc";
                switching = true;
            }
        }
    }

    // Função para baixar o arquivo HTML atualizado
    function downloadHTML() {
        const html = document.documentElement.outerHTML;
        const blob = new Blob([html], { type: 'text/html' });
        const url = URL.createObjectURL(blob);
        const a = document.createElement('a');
        a.href = url;
        a.download = 'SDA_2026_Dashboard_Clean.html';
        document.body.appendChild(a);
        a.click();
        document.body.removeChild(a);
    }
</script>

</body>
</html>
